    <h2 class="title is-2">Top Publishing Companies</h2>
    <ol class="ml-5">
    <% @publishers.each do |publisher| %>
      <li>
        <%= link_to publisher.name, publisher%>
        <em>(<%= pluralize publisher.game_count, "Top game"%>)</em>
      </li>
    <%end%>
    </ol>